These are two simple music theory drills so people can oractuce. You can
start the key signature drill with python keysigdrill.py; the other drill
is an interval drill, which you can start with python intervaldrill.py, and
either will run forever. Presently textbased, it's easily ported to pyqt,
pygtk, django.

To run the tests, use

  python -m unittest discover

or

  python3 -m unittest discover